# Evaluation: reproducing every table from the released features needs only these, and no GPU.
numpy==1.26.4
scipy==1.17.1
torch==2.5.1
PyYAML==6.0.2

# Rebuilding data/features with scripts/extract additionally needs the encoders and the readers for
# the raw scans, and a GPU. The two vote writers run in Open-YOLO3D's own environment rather than
# this one, so its further dependencies (YOLO-World, detectron2) belong to that checkout.
open_clip_torch==3.3.0
timm==1.0.27
plyfile==1.1.4
Pillow==10.2.0
imageio==2.37.3
trimesh==4.12.2
opencv-python-headless==4.13.0.92
